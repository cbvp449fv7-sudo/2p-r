import{describe,expect,it}from"vitest";import{duplicateValues,facultyImportSchema,periodSchema}from"./schemas";import{migrate}from"./storage";import{createDemoData}from"./demo";import{searchAssignments}from"./search";describe("validation, search and migration",()=>{it("rejects invalid ranks",()=>expect(facultyImportSchema.safeParse({id:"1",nameEn:"A",nameAr:"أ",rank:"Dean",specialization:"X",campus:"Y"}).success).toBe(false));it("rejects malformed periods",()=>expect(periodSchema.safeParse({day:"Sunday",start:"99:00",end:"10:00"}).success).toBe(false));it("detects duplicates",()=>expect(duplicateValues([{id:"1"},{id:"1"}],"id")).toEqual(["1"]));it("searches Arabic and combines filters",()=>{const d=createDemoData(),a=searchAssignments(d,"عضو",{faculty:"F-001"});expect(a.every(x=>x.facultyId==="F-001")).toBe(true)});it("migrates schema without discarding records",()=>{const d=createDemoData();d.schemaVersion=0;expect(migrate(d).faculty).toHaveLength(12)})});

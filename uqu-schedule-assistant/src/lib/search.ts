import type{AppData,Assignment}from"./types";export function searchAssignments(data:AppData,q:string,filters:Record<string,string>={}){const s=q.trim().toLowerCase();return data.assignments.filter(a=>{const f=data.faculty.find(x=>x.id===a.facultyId),c=data.courses.find(x=>x.code===a.courseCode),sec=data.sections.find(x=>x.id===a.sectionId),r=data.rooms.find(x=>x.id===a.roomId);const hay=[f?.id,f?.nameEn,f?.nameAr,f?.specialization,c?.code,c?.nameEn,c?.nameAr,c?.program,sec?.id,r?.id,r?.building].join(" ").toLowerCase();return(!s||hay.includes(s))&&(!filters.faculty||a.facultyId===filters.faculty)&&(!filters.section||a.sectionId===filters.section)&&(!filters.room||a.roomId===filters.room)})}

"use client";
import Link from"next/link";import{usePathname}from"next/navigation";import{useState}from"react";import{CalendarDays,LayoutDashboard,Upload,Users,BookOpen,DoorOpen,ShieldCheck,History,FileBarChart,Menu,X,Languages,Sun,Moon,Settings2}from"lucide-react";import{useApp}from"./app-provider";
const items=[["/","Dashboard",LayoutDashboard],["/import","Data Import",Upload],["/generator","Schedule Generator",Settings2],["/editor","Schedule Editor",CalendarDays],["/faculty","Faculty",Users],["/courses","Courses & Sections",BookOpen],["/rooms","Rooms",DoorOpen],["/rules","Rules & Settings",ShieldCheck],["/versions","Versions",History],["/reports","Reports",FileBarChart]]as const;
const ar:Record<string,string>={Dashboard:"لوحة المعلومات","Data Import":"استيراد البيانات","Schedule Generator":"مولد الجدول","Schedule Editor":"محرر الجدول",Faculty:"أعضاء هيئة التدريس","Courses & Sections":"المقررات والشعب",Rooms:"القاعات","Rules & Settings":"القواعد والإعدادات",Versions:"الإصدارات",Reports:"التقارير"};
export function Shell({children}:{children:React.ReactNode}){const p=usePathname(),[open,setOpen]=useState(false),{locale,setLocale,theme,setTheme,toast}=useApp();return <div className="app-shell"><button className="mobile-menu icon-btn" aria-label="Open navigation" onClick={()=>setOpen(true)}><Menu/></button>{open&&<button className="scrim" aria-label="Close navigation" onClick={()=>setOpen(false)}/>}<aside className={open?"sidebar open":"sidebar"}><div className="brand"><div className="brand-mark">UQ</div><div><strong>UQU Schedule</strong><small>Assistant · MVP</small></div><button className="close icon-btn" onClick={()=>setOpen(false)} aria-label="Close"><X/></button></div><nav>{items.map(([href,label,Icon])=><Link key={href} href={href} onClick={()=>setOpen(false)} className={p===href?"active":""}><Icon aria-hidden="true"/><span>{locale==="ar"?ar[label]:label}</span></Link>)}</nav><div className="sidebar-foot"><p>{locale==="ar"?"نموذج داخلي — ليس نظاماً رسمياً":"Internal prototype — not an official UQU system"}</p></div></aside><main><header><div><p className="eyebrow">{locale==="ar"?"قسم اللغة الإنجليزية · بيانات تجريبية":"English Department · fictional data"}</p></div><div className="header-actions"><button className="icon-btn" onClick={()=>setTheme(theme==="dark"?"light":"dark")} aria-label="Toggle theme">{theme==="dark"?<Sun/>:<Moon/>}</button><button className="lang-btn" onClick={()=>setLocale(locale==="en"?"ar":"en")}><Languages/>{locale==="en"?"العربية":"English"}</button></div></header>{toast&&<div className="toast" role="status">{toast}</div>}<div className="page">{children}</div></main></div>}
